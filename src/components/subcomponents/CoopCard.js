import React from "react";

export default function CoopCard({ title }) {

    return(

        <div className="cooperation__list__item">
            <p className="cooperation__list__item__title">{title}</p>
        </div>

    )

}